# Frontend
This folder containers the frontend code of TrackMe products. Although the frontend is already packed in the API, anyone can compile it and run it from the scratch. 

## Dependencies
npm or yarn to install the dependencies and parcel to transpile it.

## Deployment
`yarn install` or `npm install` to install the dependencies. `parcel src/index.html` to build and expose the frontend.