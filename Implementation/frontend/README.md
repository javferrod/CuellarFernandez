#Frontend
This folder containers the frontend code of TrackMe products.

##Dependencies
npm or yarn to install the dependencies and parcel to transpile it.

##Deployment
`yarn install` or `npm install` to install the dependencies. `parcel src/index.html` to build and expose the frontend.
